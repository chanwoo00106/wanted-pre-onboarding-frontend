import styled from '@emotion/styled'

const Input = styled.input`
  border: none;
  border-bottom: 0.15rem solid #ababab;
  background: transparent;
  outline: none;
  padding: 0.4rem 0.5rem;
  width: 100%;
`
export default Input
